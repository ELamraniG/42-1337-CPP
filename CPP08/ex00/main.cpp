#include <iostream>
#include <vector>
using namespace std;
int main() {
    

	vector<int>::iterator v;

	v3.insert(v3.begin() +5,155);
	v = v3.begin();
	int i = 0;
    while(v != v3.end()) 
	{
		if (v[0] == 155)
			return 0;
		std::cout<<v[i]<<std::endl;
		i++;
		v++;
    }
    
    return 0;
}




{
	sp.addNumber(6);
	sp.addNumber(3);
	sp.addNumber(11);
	sp.addNumber(17);
	sp.addNumber(9);
}